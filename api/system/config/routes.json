{
  "routes": [
    {
      "method": "GET",
      "path": "/version.txt",
      "handler": "System.version",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/version",
      "handler": "System.version",
      "config": {
        "policies": []
      }
    }
  ]
}
