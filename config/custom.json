{
  "googleMaps": {
    "apiKey": "${process.env.GOOGLE_MAPS_API_KEY || ''}"
  },
  "api": {
    "userAgent": "${process.env.API_USER_AGENT || 'Jibrel CMS'}",
    "baseUrl": "${process.env.API_BASE_URL}",
    "authToken": "${process.env.API_AUTH_TOKEN}"
  },
  "scss": {
    "src": "/scss",
    "dest": "/styles"
  },
  "internal": {
    "secret": "${process.env.INTERNAL_SECRET}"
  },
  "origin": {
    "self": "${process.env.ORIGIN_SELF || 'https://jibrel.com'}",
    "id": "${process.env.ORIGIN_ID || ''}",
    "investor": "${process.env.ORIGIN_INVESTOR || ''}",
    "company": "${process.env.ORIGIN_COMPANY || ''}"
  },
  "hostname": {
    "shared": "${process.env.HOSTNAME_SHARED || ''}"
  }
}
